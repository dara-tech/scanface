# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local development:
MONGODB_URI=mongodb://localhost:27017/attendance_app

# For MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/attendance_app?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
# For local development, use your local IP or leave as * for all origins
# For production, specify your Flutter app's allowed origins (comma-separated)
CORS_ORIGIN=*

# Example for production with specific origins:
# CORS_ORIGIN=https://your-app.com,https://www.your-app.com
